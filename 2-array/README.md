## Lab 2
Dynamic array.

Disclaimer: a lot of basic tests were generated with AI but proofread.  

### Building
Can be built using CMake, requires compiler that supports at least C++20.

```bash
mkdir build && cd build
cmake .. 
make #or ninja or VS depending on your default generator
```